# 🧹 Руководство по Полной Очистке Системы

## Обзор

Скрипт `full-system-cleanup.ts` выполняет комплексную очистку всей системы AAM, включая:
- **База данных (PostgreSQL)**: Клиенты, встречи и все связанные записи
- **Хранилище (S3/MinIO)**: Все загруженные аудио файлы
- **Очередь задач (Redis/BullMQ)**: Все задачи и блокировки

## Что удаляется

### ✅ Будет удалено:
- `Client` - все клиенты
- `Meeting` - все встречи
- `MeetingParticipant` - участники встреч
- `MeetingViewer` - наблюдатели встреч
- `UploadBlob` - метаданные загруженных файлов
- `Transcript` - транскрипты
- `Artifacts` - артефакты (результаты LLM)
- `Validation` - валидации
- `ProcessingError` - ошибки обработки
- `LLMInteraction` - история взаимодействий с LLM
- Файлы из S3/MinIO
- Задачи из Redis очереди
- Блокировки Redis

### 🔒 Будет сохранено:
- `User` - пользователи
- `MeetingType` - типы встреч
- `PromptScenario` - сценарии промптов
- `DirectoryParticipant` - справочник участников
- `UserMeetingType` - связи пользователей с типами встреч

## Использование

### 1. Просмотр того, что будет удалено (безопасно)

```bash
npx tsx scripts/full-system-cleanup.ts --dry-run
```

Этот режим **НЕ удаляет данные**, а только показывает статистику того, что будет удалено.

### 2. Удаление всех данных

```bash
npx tsx scripts/full-system-cleanup.ts --confirm
```

⚠️ **ВНИМАНИЕ**: Это удалит ВСЕ клиенты, встречи и связанные данные!

### 3. Удаление данных конкретного клиента

```bash
npx tsx scripts/full-system-cleanup.ts --client cli1 --confirm
```

Удаляет только данные указанного клиента и его встречи.

### 4. Интерактивный режим (без --confirm)

```bash
npx tsx scripts/full-system-cleanup.ts
```

Скрипт покажет статистику и попросит подтверждение перед удалением.

## Опции командной строки

| Опция | Описание |
|-------|----------|
| `--all` | Удалить все данные (используется по умолчанию) |
| `--client <id>` | Удалить данные конкретного клиента (например: `--client cli1`) |
| `--confirm` | Пропустить запрос подтверждения |
| `--dry-run` | Показать что будет удалено БЕЗ реального удаления |

## Примеры использования

### Пример 1: Безопасная проверка перед очисткой

```bash
# Шаг 1: Посмотреть что будет удалено
npx tsx scripts/full-system-cleanup.ts --dry-run

# Шаг 2: Если всё правильно, выполнить очистку
npx tsx scripts/full-system-cleanup.ts --confirm
```

### Пример 2: Удаление данных тестового клиента

```bash
# Посмотреть данные конкретного клиента
npx tsx scripts/full-system-cleanup.ts --client cli1 --dry-run

# Удалить если всё правильно
npx tsx scripts/full-system-cleanup.ts --client cli1 --confirm
```

### Пример 3: Полная очистка для нового старта

```bash
# Удалить ВСЕ данные
npx tsx scripts/full-system-cleanup.ts --confirm
```

## Процесс очистки

Скрипт выполняет очистку в следующем порядке:

1. **Сбор статистики** - показывает текущее состояние
2. **Очистка Redis блокировок** - снимает все блокировки `lock:meeting:*`
3. **Очистка Redis задач** - удаляет все задачи из очереди BullMQ
4. **Удаление файлов из S3** - удаляет все загруженные файлы
5. **Удаление из БД** - удаляет встречи (каскадно) и клиентов
6. **Проверка целостности** - проверяет что всё удалено корректно
7. **Финальная статистика** - показывает результаты

## Безопасность

### ✅ Безопасные операции:
- `--dry-run` - **безопасно**, ничего не удаляет
- Без `--confirm` - попросит подтверждение

### ⚠️ Опасные операции:
- `--confirm` - удаляет данные БЕЗ подтверждения
- Удаление **необратимо**

### 🛡️ Рекомендации:
1. **ВСЕГДА** сначала запускайте с `--dry-run`
2. Проверьте статистику перед удалением
3. Создайте резервную копию БД если данные важны
4. Используйте `--client` для удаления отдельных клиентов

## Вывод скрипта

### Статистика BEFORE CLEANUP
Показывает текущее состояние системы:
- Количество записей в БД по каждой таблице
- Количество файлов в S3 и их размер
- Количество задач и блокировок в Redis

### Процесс очистки
Показывает прогресс каждого шага:
- ✅ успешное выполнение
- ⚠️ предупреждения
- ❌ ошибки

### Статистика AFTER CLEANUP
Показывает финальное состояние после очистки

## Устранение проблем

### Проблема: "S3 configuration is missing"
**Решение**: Проверьте `.env` файл, убедитесь что установлены:
- `S3_ENDPOINT`
- `S3_ACCESS_KEY_ID`
- `S3_SECRET_ACCESS_KEY`
- `S3_BUCKET_NAME`

### Проблема: "Redis connection failed"
**Решение**: Убедитесь что Redis запущен и доступен по адресу из `REDIS_URL`

### Проблема: "Some records may still exist"
**Решение**: Это может быть нормально если:
- Использовался флаг `--client` (удалялся только один клиент)
- Есть ошибки прав доступа к S3
- Проверьте логи на наличие ошибок

## Альтернативные скрипты

Если нужна более точечная очистка, используйте:

- `clean-database.ts` - очистка только БД и S3 (без Redis)
- `cleanup-meetings.ts` - очистка только встреч (без S3)
- `delete-meeting.ts <id>` - удаление одной встречи
- `cleanup-old-jobs.ts` - очистка только старых задач из Redis

## Восстановление после очистки

После очистки система готова к работе:
- ✅ Пользователи остаются
- ✅ Типы встреч остаются
- ✅ Сценарии промптов остаются
- ✅ База участников остаётся

Можно сразу начинать создавать новых клиентов и встречи.

## Поддержка

При возникновении проблем:
1. Запустите с `--dry-run` и проверьте вывод
2. Проверьте логи воркера и БД
3. Убедитесь что все сервисы (PostgreSQL, Redis, MinIO) запущены
4. Проверьте права доступа к БД и S3

---

**⚠️ ВАЖНО**: Эта операция необратима. Всегда делайте резервную копию перед удалением важных данных!

